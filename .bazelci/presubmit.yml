---
tasks:
  ubuntu_1804:
    platform: ubuntu1804
    shell_commands:
      - "sudo apt update && sudo apt -y install bison flex uuid-dev asciidoc graphviz source-highlight"
    build_targets:
      - "//kythe/..."
    test_targets:
      - "--"
      - "//kythe/..."
