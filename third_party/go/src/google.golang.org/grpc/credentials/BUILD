package(default_visibility = ["//visibility:public"])

load("//third_party/go:build.bzl", "gopath_package")

licenses(["notice"])

gopath_package(
    deps = [
        "@go_x_net//:context",
        "@go_oauth2//:oauth2",
        "@go_oauth2//:google",
        "@go_oauth2//:jwt",
    ],
)
